[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "programming-python"
authors = [{name = "Allan Avendaño", email = "aavendan@espol.edu.ec"}]
dynamic = ["version", "description"]
